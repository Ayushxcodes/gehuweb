-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.app_achievement_certificates (
  certificate_id text NOT NULL CHECK (length(TRIM(BOTH FROM certificate_id)) > 0),
  verify_code text NOT NULL UNIQUE CHECK (length(TRIM(BOTH FROM verify_code)) > 0),
  student_id text NOT NULL,
  auth_user_id uuid,
  category text NOT NULL CHECK (category = ANY (ARRAY['COURSE'::text, 'OTHER'::text, 'ACHIEVEMENT'::text])),
  title text NOT NULL CHECK (length(TRIM(BOTH FROM title)) > 0),
  issuer_name text NOT NULL DEFAULT ''::text,
  description text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'VERIFIED'::text,
  certificate_position text NOT NULL DEFAULT ''::text,
  storage_url text NOT NULL DEFAULT ''::text,
  storage_object_key text NOT NULL DEFAULT ''::text,
  certificate_version text NOT NULL DEFAULT ''::text,
  issued_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone,
  created_by_auth_user_id uuid,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_achievement_certificates_pkey PRIMARY KEY (certificate_id),
  CONSTRAINT app_achievement_certificates_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT app_achievement_certificates_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT app_achievement_certificates_created_by_auth_user_id_fkey FOREIGN KEY (created_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.app_appeals (
  appeal_id text NOT NULL,
  uid text NOT NULL,
  name text NOT NULL DEFAULT ''::text,
  email text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT 'PROFILE_CHANGE'::text,
  message text NOT NULL,
  status text NOT NULL DEFAULT 'PENDING'::text CHECK (status = ANY (ARRAY['PENDING'::text, 'RESOLVED'::text, 'REJECTED'::text, 'CANCELLED'::text])),
  profile_path text NOT NULL DEFAULT ''::text,
  roll_no text NOT NULL DEFAULT ''::text,
  course text NOT NULL DEFAULT ''::text,
  semester integer,
  profile_data_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  admin_note text NOT NULL DEFAULT ''::text,
  resolved_by_auth_user_id uuid,
  resolved_at timestamp with time zone,
  source_created_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_appeals_pkey PRIMARY KEY (appeal_id),
  CONSTRAINT app_appeals_resolved_by_auth_user_id_fkey FOREIGN KEY (resolved_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.app_directory_index (
  uid text NOT NULL,
  name text NOT NULL DEFAULT ''::text,
  email text NOT NULL DEFAULT ''::text,
  roll_no text NOT NULL DEFAULT ''::text,
  university_roll text NOT NULL DEFAULT ''::text,
  role text NOT NULL DEFAULT ''::text,
  email_key text NOT NULL DEFAULT ''::text CHECK (email_key = lower(TRIM(BOTH FROM email_key))),
  roll_no_key text NOT NULL DEFAULT ''::text CHECK (roll_no_key = upper(TRIM(BOTH FROM roll_no_key)) OR roll_no_key = ''::text),
  university_roll_key text NOT NULL DEFAULT ''::text CHECK (university_roll_key = upper(TRIM(BOTH FROM university_roll_key)) OR university_roll_key = ''::text),
  student_id text NOT NULL DEFAULT ''::text,
  source_updated_at timestamp with time zone,
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_directory_index_pkey PRIMARY KEY (uid)
);
CREATE TABLE public.app_fcm_tokens (
  token text NOT NULL CHECK (length(TRIM(BOTH FROM token)) > 0),
  uid text NOT NULL,
  auth_user_id uuid,
  student_id text,
  platform text NOT NULL CHECK (platform = ANY (ARRAY['ANDROID'::text, 'WEB'::text])),
  device_id text NOT NULL DEFAULT ''::text,
  app_version text NOT NULL DEFAULT ''::text,
  user_agent text NOT NULL DEFAULT ''::text,
  is_active boolean NOT NULL DEFAULT true,
  last_seen_at timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_fcm_tokens_pkey PRIMARY KEY (token),
  CONSTRAINT app_fcm_tokens_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.app_inbox (
  uid text NOT NULL,
  inbox_id text NOT NULL,
  type text NOT NULL DEFAULT 'GENERAL'::text,
  title text NOT NULL DEFAULT ''::text,
  body text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'UNREAD'::text CHECK (status = ANY (ARRAY['UNREAD'::text, 'READ'::text, 'ARCHIVED'::text, 'DISMISSED'::text])),
  event_id text NOT NULL DEFAULT ''::text,
  comp_id text NOT NULL DEFAULT ''::text,
  team_id text NOT NULL DEFAULT ''::text,
  notice_id text NOT NULL DEFAULT ''::text,
  from_uid text NOT NULL DEFAULT ''::text,
  target_id text NOT NULL DEFAULT ''::text,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  read_at timestamp with time zone,
  source_created_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_inbox_pkey PRIMARY KEY (uid, inbox_id)
);
CREATE TABLE public.app_mock_questions (
  question_id uuid NOT NULL DEFAULT gen_random_uuid(),
  test_id uuid NOT NULL,
  subject text NOT NULL,
  topic text NOT NULL,
  question text NOT NULL,
  option_a text NOT NULL,
  option_b text NOT NULL,
  option_c text NOT NULL,
  option_d text NOT NULL,
  answer text NOT NULL,
  difficulty text NOT NULL,
  solution text,
  active boolean NOT NULL DEFAULT true,
  CONSTRAINT app_mock_questions_pkey PRIMARY KEY (question_id),
  CONSTRAINT app_mock_questions_test_id_fkey FOREIGN KEY (test_id) REFERENCES public.app_mock_tests(test_id)
);
CREATE TABLE public.app_mock_results (
  result_id bigint NOT NULL DEFAULT nextval('app_mock_results_result_id_seq'::regclass),
  test_id uuid NOT NULL,
  auth_user_id uuid,
  student_id text NOT NULL,
  student_name text NOT NULL DEFAULT ''::text,
  roll_no text NOT NULL DEFAULT ''::text,
  branch text NOT NULL DEFAULT ''::text,
  course text NOT NULL DEFAULT ''::text,
  semester text NOT NULL DEFAULT ''::text,
  locked boolean NOT NULL DEFAULT false,
  submitted_at timestamp with time zone NOT NULL DEFAULT now(),
  score real NOT NULL DEFAULT 0,
  max_marks integer NOT NULL DEFAULT 0,
  percentage real NOT NULL DEFAULT 0,
  published boolean NOT NULL DEFAULT false,
  CONSTRAINT app_mock_results_pkey PRIMARY KEY (result_id),
  CONSTRAINT app_mock_results_test_id_fkey FOREIGN KEY (test_id) REFERENCES public.app_mock_tests(test_id),
  CONSTRAINT app_mock_results_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT app_mock_results_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.app_mock_tests (
  test_id uuid NOT NULL DEFAULT gen_random_uuid(),
  title text NOT NULL,
  start_at timestamp with time zone NOT NULL,
  duration_minutes integer NOT NULL DEFAULT 60,
  requires_web_proctoring boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_mock_tests_pkey PRIMARY KEY (test_id)
);
CREATE TABLE public.app_notice_attachments (
  attachment_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  notice_id text NOT NULL,
  sort_order integer NOT NULL DEFAULT 0,
  name text NOT NULL DEFAULT ''::text,
  url text NOT NULL DEFAULT ''::text,
  mime text NOT NULL DEFAULT ''::text,
  size_bytes bigint NOT NULL DEFAULT 0 CHECK (size_bytes >= 0),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_notice_attachments_pkey PRIMARY KEY (attachment_id),
  CONSTRAINT app_notice_attachments_notice_id_fkey FOREIGN KEY (notice_id) REFERENCES public.app_notices(notice_id)
);
CREATE TABLE public.app_notice_reads (
  uid text NOT NULL,
  notice_id text NOT NULL,
  read_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_notice_reads_pkey PRIMARY KEY (uid, notice_id)
);
CREATE TABLE public.app_notices (
  notice_id text NOT NULL,
  title text NOT NULL DEFAULT ''::text,
  body text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT 'holiday'::text CHECK (type = ANY (ARRAY['holiday'::text, 'event'::text, 'job'::text])),
  branch text NOT NULL DEFAULT 'ALL'::text,
  courses ARRAY NOT NULL DEFAULT ARRAY['ALL'::text],
  semesters ARRAY NOT NULL DEFAULT ARRAY['-1'::integer],
  created_by_uid text NOT NULL DEFAULT ''::text,
  active boolean NOT NULL DEFAULT true,
  expires_at timestamp with time zone,
  registration_deadline timestamp with time zone,
  event_template text,
  event_config_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  event_id text NOT NULL DEFAULT ''::text,
  cta_label text NOT NULL DEFAULT ''::text,
  participation_enabled boolean NOT NULL DEFAULT false,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  source_created_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_notices_pkey PRIMARY KEY (notice_id)
);
CREATE TABLE public.app_notification_meta (
  uid text NOT NULL,
  last_seen_at timestamp with time zone,
  dismissed_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  muted boolean NOT NULL DEFAULT false,
  meta_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_notification_meta_pkey PRIMARY KEY (uid)
);
CREATE TABLE public.app_notifications (
  notif_id text NOT NULL,
  type text NOT NULL DEFAULT 'NOTICE'::text,
  title text NOT NULL DEFAULT ''::text,
  message text NOT NULL DEFAULT ''::text,
  target_type text NOT NULL DEFAULT 'ALL'::text CHECK (target_type = ANY (ARRAY['ALL'::text, 'SEGMENT'::text, 'UID'::text, 'EVENT'::text, 'CUSTOM'::text])),
  target_key text NOT NULL DEFAULT 'all_students'::text,
  target_id text NOT NULL DEFAULT ''::text,
  priority text NOT NULL DEFAULT 'NORMAL'::text CHECK (priority = ANY (ARRAY['LOW'::text, 'NORMAL'::text, 'HIGH'::text, 'URGENT'::text])),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  is_active boolean NOT NULL DEFAULT true,
  source_created_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_notifications_pkey PRIMARY KEY (notif_id)
);
CREATE TABLE public.app_official_feedback (
  uid text NOT NULL,
  feedback_id text NOT NULL,
  message text NOT NULL,
  admin_name text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT ''::text,
  source_timestamp timestamp with time zone,
  meta_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_official_feedback_pkey PRIMARY KEY (uid, feedback_id)
);
CREATE TABLE public.app_profile_state (
  uid text NOT NULL,
  auth_user_id uuid UNIQUE,
  student_id text UNIQUE,
  employee_id text UNIQUE,
  account_role text NOT NULL DEFAULT 'STUDENT'::text CHECK (account_role = ANY (ARRAY['STUDENT'::text, 'ADMIN'::text, 'COORDINATOR'::text, 'EMPLOYEE'::text])),
  name text NOT NULL DEFAULT ''::text,
  official_email text NOT NULL DEFAULT ''::text,
  official_email_key text NOT NULL DEFAULT ''::text CHECK (official_email_key = lower(TRIM(BOTH FROM official_email_key))),
  personal_email text,
  personal_email_key text NOT NULL DEFAULT ''::text CHECK (personal_email_key = ''::text OR personal_email_key = lower(TRIM(BOTH FROM personal_email_key))),
  student_id_label text NOT NULL DEFAULT ''::text,
  roll_no text NOT NULL DEFAULT ''::text,
  roll_number_legacy text NOT NULL DEFAULT ''::text,
  university_roll text NOT NULL DEFAULT ''::text,
  enrollment_no text NOT NULL DEFAULT ''::text,
  course text NOT NULL DEFAULT ''::text,
  branch text NOT NULL DEFAULT ''::text,
  semester integer CHECK (semester IS NULL OR semester >= 1 AND semester <= 20),
  dob_text text NOT NULL DEFAULT ''::text,
  gender text NOT NULL DEFAULT ''::text,
  category text NOT NULL DEFAULT ''::text,
  father_name text NOT NULL DEFAULT ''::text,
  father_occupation text NOT NULL DEFAULT ''::text,
  father_mobile text NOT NULL DEFAULT ''::text,
  mother_name text NOT NULL DEFAULT ''::text,
  mother_occupation text NOT NULL DEFAULT ''::text,
  mother_mobile text NOT NULL DEFAULT ''::text,
  student_mobile text NOT NULL DEFAULT ''::text,
  phone text NOT NULL DEFAULT ''::text,
  address_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  academic_record_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  photo_url text NOT NULL DEFAULT ''::text,
  photo_path text NOT NULL DEFAULT ''::text,
  profile_completed boolean NOT NULL DEFAULT false,
  verification_status text NOT NULL DEFAULT 'PENDING'::text CHECK (verification_status = ANY (ARRAY['PENDING'::text, 'VERIFIED'::text, 'REJECTED'::text, 'LOCKED'::text, 'DRAFT'::text])),
  verified boolean NOT NULL DEFAULT false,
  edit_unlocked_until timestamp with time zone,
  lock_reason text NOT NULL DEFAULT ''::text,
  fcm_token text NOT NULL DEFAULT ''::text,
  source_created_at timestamp with time zone,
  source_updated_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_profile_state_pkey PRIMARY KEY (uid),
  CONSTRAINT app_profile_state_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT app_profile_state_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT app_profile_state_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.app_push_queue (
  push_id text NOT NULL CHECK (length(TRIM(BOTH FROM push_id)) > 0),
  target_type text NOT NULL CHECK (target_type = ANY (ARRAY['USER'::text, 'EVENT'::text, 'SEGMENT'::text, 'ALL'::text])),
  target_key text NOT NULL DEFAULT ''::text,
  uid text,
  event_id text,
  comp_id text,
  notification_id text,
  title text NOT NULL CHECK (length(TRIM(BOTH FROM title)) > 0),
  body text NOT NULL CHECK (length(TRIM(BOTH FROM body)) > 0),
  route text NOT NULL DEFAULT ''::text,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  status text NOT NULL DEFAULT 'PENDING'::text CHECK (status = ANY (ARRAY['PENDING'::text, 'SENT'::text, 'FAILED'::text, 'SKIPPED'::text])),
  attempts integer NOT NULL DEFAULT 0 CHECK (attempts >= 0),
  last_error text NOT NULL DEFAULT ''::text,
  claimed_at timestamp with time zone,
  sent_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_push_queue_pkey PRIMARY KEY (push_id)
);
CREATE TABLE public.app_user_identity (
  app_identity_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  auth_user_id uuid NOT NULL UNIQUE,
  account_type text NOT NULL CHECK (account_type = ANY (ARRAY['STUDENT'::text, 'EMPLOYEE'::text, 'ADMIN'::text])),
  student_id text UNIQUE,
  employee_id text UNIQUE,
  is_active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT app_user_identity_pkey PRIMARY KEY (app_identity_id),
  CONSTRAINT app_user_identity_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT app_user_identity_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT app_user_identity_employee_id_fkey FOREIGN KEY (employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.emp_campus_master (
  emp_campus_id bigint NOT NULL DEFAULT nextval('emp_campus_master_emp_campus_id_seq'::regclass),
  emp_campus_code text NOT NULL UNIQUE,
  emp_campus_name text NOT NULL,
  emp_campus_state text,
  emp_campus_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_campus_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT emp_campus_master_pkey PRIMARY KEY (emp_campus_id)
);
CREATE TABLE public.emp_department_master (
  emp_department_id bigint NOT NULL DEFAULT nextval('emp_department_master_emp_department_id_seq'::regclass),
  emp_department_code text NOT NULL UNIQUE,
  emp_department_name text NOT NULL,
  emp_department_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_department_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT emp_department_master_pkey PRIMARY KEY (emp_department_id)
);
CREATE TABLE public.emp_designation_master (
  emp_designation_id bigint NOT NULL DEFAULT nextval('emp_designation_master_emp_designation_id_seq'::regclass),
  emp_designation_code text NOT NULL UNIQUE,
  emp_designation_name text NOT NULL,
  emp_designation_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_designation_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT emp_designation_master_pkey PRIMARY KEY (emp_designation_id)
);
CREATE TABLE public.emp_role_master (
  emp_role_id bigint NOT NULL DEFAULT nextval('emp_role_master_emp_role_id_seq'::regclass),
  emp_role_code text NOT NULL UNIQUE,
  emp_role_name text NOT NULL,
  emp_role_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_role_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT emp_role_master_pkey PRIMARY KEY (emp_role_id)
);
CREATE TABLE public.employee_address (
  emp_address_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_address_employee_id text NOT NULL UNIQUE,
  emp_permanent_line1 text,
  emp_permanent_line2 text,
  emp_city_name text,
  emp_state_name text,
  emp_pin_code text,
  emp_country_name text,
  emp_address_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_address_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_address_pkey PRIMARY KEY (emp_address_id),
  CONSTRAINT employee_address_emp_address_employee_id_fkey FOREIGN KEY (emp_address_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_attendance_daily (
  emp_attendance_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_attendance_employee_id text NOT NULL,
  emp_attendance_date date NOT NULL,
  emp_attendance_mark text NOT NULL CHECK (emp_attendance_mark = ANY (ARRAY['P'::text, 'A'::text, 'L'::text, 'OD'::text])),
  emp_attendance_marked_by text,
  emp_attendance_created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_attendance_daily_pkey PRIMARY KEY (emp_attendance_id),
  CONSTRAINT employee_attendance_daily_emp_attendance_employee_id_fkey FOREIGN KEY (emp_attendance_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_contact (
  emp_contact_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_contact_employee_id text NOT NULL UNIQUE,
  emp_official_email text NOT NULL UNIQUE CHECK (POSITION(('@'::text) IN (emp_official_email)) > 1),
  emp_personal_email text UNIQUE CHECK (emp_personal_email IS NULL OR POSITION(('@'::text) IN (emp_personal_email)) > 1),
  emp_primary_phone text NOT NULL,
  emp_alternate_phone text,
  emp_contact_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_contact_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_contact_pkey PRIMARY KEY (emp_contact_id),
  CONSTRAINT employee_contact_emp_contact_employee_id_fkey FOREIGN KEY (emp_contact_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_core (
  emp_employee_id text NOT NULL CHECK (length(TRIM(BOTH FROM emp_employee_id)) > 0),
  emp_auth_user_uuid uuid UNIQUE,
  emp_full_name text NOT NULL CHECK (length(TRIM(BOTH FROM emp_full_name)) > 1),
  emp_dob date CHECK (emp_dob <= CURRENT_DATE),
  emp_gender_code text CHECK (emp_gender_code = ANY (ARRAY['MALE'::text, 'FEMALE'::text, 'OTHER'::text, 'UNDISCLOSED'::text])),
  emp_blood_group_code text,
  emp_profile_photo_url text,
  emp_profile_photo_storage_provider text,
  emp_profile_photo_object_key text,
  emp_profile_photo_updated_at timestamp with time zone,
  emp_account_state text NOT NULL DEFAULT 'ACTIVE'::text CHECK (emp_account_state = ANY (ARRAY['ACTIVE'::text, 'ON_LEAVE'::text, 'INACTIVE'::text, 'EXITED'::text, 'SUSPENDED'::text, 'BLOCKED'::text])),
  emp_core_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_core_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_core_pkey PRIMARY KEY (emp_employee_id),
  CONSTRAINT employee_core_emp_auth_user_uuid_fkey FOREIGN KEY (emp_auth_user_uuid) REFERENCES auth.users(id)
);
CREATE TABLE public.employee_family (
  emp_family_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_family_employee_id text NOT NULL UNIQUE,
  emp_father_name text,
  emp_mother_name text,
  emp_spouse_name text,
  emp_guardian_name text,
  emp_guardian_phone text,
  emp_emergency_contact_name text,
  emp_emergency_contact_phone text,
  emp_emergency_contact_relation text,
  emp_family_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_family_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_family_pkey PRIMARY KEY (emp_family_id),
  CONSTRAINT employee_family_emp_family_employee_id_fkey FOREIGN KEY (emp_family_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_job_current (
  emp_job_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_job_employee_id text NOT NULL UNIQUE,
  emp_campus_id bigint NOT NULL,
  emp_department_id bigint NOT NULL,
  emp_designation_id bigint NOT NULL,
  emp_role_id bigint NOT NULL,
  emp_joining_date date NOT NULL,
  emp_experience_years numeric,
  emp_reporting_to_employee_id text,
  emp_employment_type_code text CHECK (emp_employment_type_code = ANY (ARRAY['FULL_TIME'::text, 'PART_TIME'::text, 'CONTRACT'::text, 'VISITING'::text, 'TEMPORARY'::text, 'OTHER'::text])),
  emp_highest_qualification text,
  emp_specialization_text text,
  emp_is_phd_guide boolean NOT NULL DEFAULT false,
  emp_job_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_job_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_job_current_pkey PRIMARY KEY (emp_job_id),
  CONSTRAINT employee_job_current_emp_job_employee_id_fkey FOREIGN KEY (emp_job_employee_id) REFERENCES public.employee_core(emp_employee_id),
  CONSTRAINT employee_job_current_emp_campus_id_fkey FOREIGN KEY (emp_campus_id) REFERENCES public.emp_campus_master(emp_campus_id),
  CONSTRAINT employee_job_current_emp_department_id_fkey FOREIGN KEY (emp_department_id) REFERENCES public.emp_department_master(emp_department_id),
  CONSTRAINT employee_job_current_emp_designation_id_fkey FOREIGN KEY (emp_designation_id) REFERENCES public.emp_designation_master(emp_designation_id),
  CONSTRAINT employee_job_current_emp_role_id_fkey FOREIGN KEY (emp_role_id) REFERENCES public.emp_role_master(emp_role_id),
  CONSTRAINT employee_job_current_emp_reporting_to_employee_id_fkey FOREIGN KEY (emp_reporting_to_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_leave_balance (
  emp_leave_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_leave_employee_id text NOT NULL,
  emp_leave_type_code text NOT NULL,
  emp_leave_year_no integer NOT NULL CHECK (emp_leave_year_no >= 2000 AND emp_leave_year_no <= 2100),
  emp_leave_total_count numeric NOT NULL DEFAULT 0 CHECK (emp_leave_total_count >= 0::numeric),
  emp_leave_used_count numeric NOT NULL DEFAULT 0 CHECK (emp_leave_used_count >= 0::numeric),
  emp_leave_balance_count numeric NOT NULL DEFAULT 0 CHECK (emp_leave_balance_count >= 0::numeric),
  emp_leave_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_leave_balance_pkey PRIMARY KEY (emp_leave_id),
  CONSTRAINT employee_leave_balance_emp_leave_employee_id_fkey FOREIGN KEY (emp_leave_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_qualification_record (
  emp_qualification_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_qualification_employee_id text NOT NULL,
  emp_qualification_level text NOT NULL,
  emp_qualification_institute_name text,
  emp_qualification_passing_year integer CHECK (emp_qualification_passing_year >= 1950 AND emp_qualification_passing_year <= 2100),
  emp_qualification_score_type text CHECK (emp_qualification_score_type = ANY (ARRAY['PERCENT'::text, 'CGPA'::text, 'GRADE'::text, 'NA'::text])),
  emp_qualification_score_value numeric,
  emp_qualification_created_at timestamp with time zone NOT NULL DEFAULT now(),
  emp_qualification_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_qualification_record_pkey PRIMARY KEY (emp_qualification_id),
  CONSTRAINT employee_qualification_record_emp_qualification_employee_i_fkey FOREIGN KEY (emp_qualification_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.employee_status_log (
  emp_status_log_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  emp_status_employee_id text NOT NULL,
  emp_prev_state text,
  emp_next_state text NOT NULL,
  emp_state_reason text,
  emp_state_changed_by text,
  emp_state_changed_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT employee_status_log_pkey PRIMARY KEY (emp_status_log_id),
  CONSTRAINT employee_status_log_emp_status_employee_id_fkey FOREIGN KEY (emp_status_employee_id) REFERENCES public.employee_core(emp_employee_id)
);
CREATE TABLE public.event_announcements (
  event_id text NOT NULL,
  announcement_id text NOT NULL,
  message text NOT NULL,
  sender_name text NOT NULL DEFAULT ''::text,
  sender_auth_user_id uuid,
  type text NOT NULL DEFAULT 'GENERAL'::text,
  topics jsonb NOT NULL DEFAULT '[]'::jsonb,
  sent_at timestamp with time zone NOT NULL DEFAULT now(),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_announcements_pkey PRIMARY KEY (event_id, announcement_id),
  CONSTRAINT event_announcements_sender_auth_user_id_fkey FOREIGN KEY (sender_auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_announcements_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_core(event_id)
);
CREATE TABLE public.event_attendance (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  status text NOT NULL DEFAULT 'PARTIAL'::text CHECK (status = ANY (ARRAY['FULL'::text, 'PARTIAL'::text, 'ABSENT'::text])),
  present_count integer NOT NULL DEFAULT 0 CHECK (present_count >= 0),
  total_members integer NOT NULL DEFAULT 0 CHECK (total_members >= 0),
  marked_by_auth_user_id uuid,
  marked_at timestamp with time zone NOT NULL DEFAULT now(),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  team_name text NOT NULL DEFAULT ''::text,
  present_student_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  absent_student_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  CONSTRAINT event_attendance_pkey PRIMARY KEY (event_id, comp_id, team_id),
  CONSTRAINT event_attendance_marked_by_auth_user_id_fkey FOREIGN KEY (marked_by_auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_attendance_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(team_id)
);
CREATE TABLE public.event_attendance_members (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  student_id text NOT NULL,
  present boolean NOT NULL DEFAULT false,
  marked_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_attendance_members_pkey PRIMARY KEY (event_id, comp_id, team_id, student_id),
  CONSTRAINT event_attendance_members_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_attendance(event_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_attendance(event_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_attendance(event_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_attendance(comp_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_attendance(comp_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_attendance(comp_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_attendance(team_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_attendance(team_id),
  CONSTRAINT event_attendance_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_attendance(team_id)
);
CREATE TABLE public.event_certificates (
  certificate_id text NOT NULL,
  verify_code text NOT NULL UNIQUE,
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL DEFAULT ''::text,
  student_id text NOT NULL,
  student_name text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'PARTICIPANT'::text,
  position text NOT NULL DEFAULT ''::text,
  storage_url text NOT NULL DEFAULT ''::text,
  storage_object_key text NOT NULL DEFAULT ''::text,
  certificate_version text NOT NULL DEFAULT ''::text,
  issued_at timestamp with time zone NOT NULL DEFAULT now(),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  certificate_position text NOT NULL DEFAULT ''::text,
  auth_user_id uuid,
  event_title text NOT NULL DEFAULT ''::text,
  competition_name text NOT NULL DEFAULT ''::text,
  CONSTRAINT event_certificates_pkey PRIMARY KEY (certificate_id),
  CONSTRAINT event_certificates_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_certificates_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.event_competitions (
  event_id text NOT NULL,
  comp_id text NOT NULL CHECK (length(TRIM(BOTH FROM comp_id)) > 0),
  competition_name text NOT NULL CHECK (length(TRIM(BOTH FROM competition_name)) > 0),
  subtitle text NOT NULL DEFAULT ''::text,
  is_default boolean NOT NULL DEFAULT false,
  is_live boolean NOT NULL DEFAULT false,
  participation_mode text NOT NULL DEFAULT 'BOTH'::text CHECK (participation_mode = ANY (ARRAY['SOLO'::text, 'TEAM'::text, 'BOTH'::text])),
  max_members integer NOT NULL DEFAULT 1 CHECK (max_members >= 1 AND max_members <= 50),
  team_config jsonb NOT NULL DEFAULT '{}'::jsonb,
  solo_config jsonb NOT NULL DEFAULT '{}'::jsonb,
  qr_enabled boolean NOT NULL DEFAULT true,
  venue text NOT NULL DEFAULT ''::text,
  whatsapp_group_link text NOT NULL DEFAULT ''::text,
  organized_by text NOT NULL DEFAULT ''::text,
  coordinator_uids ARRAY NOT NULL DEFAULT '{}'::text[],
  scanner_uids ARRAY NOT NULL DEFAULT '{}'::text[],
  is_free boolean NOT NULL DEFAULT true,
  fee_amount numeric NOT NULL DEFAULT 0 CHECK (fee_amount >= 0::numeric),
  fee_instructions text NOT NULL DEFAULT ''::text,
  payment_method text NOT NULL DEFAULT 'MANUAL'::text CHECK (payment_method = ANY (ARRAY['MANUAL'::text, 'RAZORPAY'::text, 'ERP_GATEWAY'::text])),
  registration_deadline timestamp with time zone,
  results_published boolean NOT NULL DEFAULT false,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  min_members integer NOT NULL DEFAULT 1 CHECK (min_members >= 1 AND min_members <= 50),
  max_participants integer CHECK (max_participants IS NULL OR max_participants >= 0),
  max_teams integer CHECK (max_teams IS NULL OR max_teams >= 0),
  categories ARRAY NOT NULL DEFAULT '{}'::text[],
  reg_count integer NOT NULL DEFAULT 0 CHECK (reg_count >= 0),
  attendance_open boolean NOT NULL DEFAULT false,
  attendance_locked boolean NOT NULL DEFAULT false,
  results_locked_at timestamp with time zone,
  CONSTRAINT event_competitions_pkey PRIMARY KEY (event_id, comp_id),
  CONSTRAINT event_competitions_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_core(event_id)
);
CREATE TABLE public.event_core (
  event_id text NOT NULL CHECK (length(TRIM(BOTH FROM event_id)) > 0),
  title text NOT NULL CHECK (length(TRIM(BOTH FROM title)) > 0),
  subtitle text NOT NULL DEFAULT ''::text,
  description text NOT NULL DEFAULT ''::text,
  organized_by text NOT NULL DEFAULT ''::text,
  whatsapp_link text NOT NULL DEFAULT ''::text,
  banner_url text NOT NULL DEFAULT ''::text,
  banner_object_key text NOT NULL DEFAULT ''::text,
  event_type text NOT NULL DEFAULT 'MAIN'::text CHECK (event_type = ANY (ARRAY['MAIN'::text, 'STANDALONE'::text])),
  is_live boolean NOT NULL DEFAULT false,
  finalized boolean NOT NULL DEFAULT false,
  is_cancelled boolean NOT NULL DEFAULT false,
  event_interaction_locked boolean NOT NULL DEFAULT false,
  start_at timestamp with time zone,
  end_at timestamp with time zone,
  registration_deadline timestamp with time zone,
  venue text NOT NULL DEFAULT ''::text,
  target_campuses ARRAY NOT NULL DEFAULT ARRAY['ALL'::text],
  target_courses ARRAY NOT NULL DEFAULT ARRAY['ALL'::text],
  target_semesters ARRAY NOT NULL DEFAULT ARRAY['-1'::integer],
  audience_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  total_registrations integer NOT NULL DEFAULT 0 CHECK (total_registrations >= 0),
  competitions_count integer NOT NULL DEFAULT 0 CHECK (competitions_count >= 0),
  created_by_uid text NOT NULL DEFAULT ''::text,
  created_by_auth_user_id uuid,
  source text NOT NULL DEFAULT 'supabase'::text,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  source_created_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_core_pkey PRIMARY KEY (event_id),
  CONSTRAINT event_core_created_by_auth_user_id_fkey FOREIGN KEY (created_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.event_group_channels (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  group_id text NOT NULL DEFAULT 'main'::text,
  active boolean NOT NULL DEFAULT true,
  member_student_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_group_channels_pkey PRIMARY KEY (event_id, comp_id, group_id),
  CONSTRAINT event_group_channels_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_group_channels_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_group_channels_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(comp_id),
  CONSTRAINT event_group_channels_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(comp_id)
);
CREATE TABLE public.event_group_members (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  group_id text NOT NULL DEFAULT 'main'::text,
  student_id text NOT NULL,
  role text NOT NULL DEFAULT 'MEMBER'::text,
  status text NOT NULL DEFAULT 'ACTIVE'::text,
  joined_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_group_members_pkey PRIMARY KEY (event_id, comp_id, group_id, student_id),
  CONSTRAINT event_group_members_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(group_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(group_id),
  CONSTRAINT event_group_members_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(group_id)
);
CREATE TABLE public.event_group_messages (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  group_id text NOT NULL DEFAULT 'main'::text,
  message_id text NOT NULL,
  sender_student_id text,
  sender_name text NOT NULL DEFAULT ''::text,
  sender_role text NOT NULL DEFAULT ''::text,
  text text NOT NULL DEFAULT ''::text,
  type text NOT NULL DEFAULT 'TEXT'::text,
  sent_at timestamp with time zone NOT NULL DEFAULT now(),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT event_group_messages_pkey PRIMARY KEY (event_id, comp_id, group_id, message_id),
  CONSTRAINT event_group_messages_sender_student_id_fkey FOREIGN KEY (sender_student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(event_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(comp_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_group_channels(group_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_group_channels(group_id),
  CONSTRAINT event_group_messages_event_id_comp_id_group_id_fkey FOREIGN KEY (group_id) REFERENCES public.event_group_channels(group_id)
);
CREATE TABLE public.event_participants (
  event_id text NOT NULL,
  student_id text NOT NULL,
  auth_user_id uuid,
  legacy_firebase_uid text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'REGISTERED'::text,
  comp_ids ARRAY NOT NULL DEFAULT '{}'::text[],
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_participants_pkey PRIMARY KEY (event_id, student_id),
  CONSTRAINT event_participants_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_core(event_id),
  CONSTRAINT event_participants_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_participants_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.event_payment_records (
  payment_record_id text NOT NULL,
  event_id text NOT NULL,
  comp_id text NOT NULL,
  student_id text NOT NULL,
  auth_user_id uuid,
  legacy_firebase_uid text NOT NULL DEFAULT ''::text,
  status text NOT NULL DEFAULT 'PAYMENT_PENDING'::text CHECK (status = ANY (ARRAY['PAYMENT_PENDING'::text, 'PAYMENT_SUBMITTED'::text, 'PAYMENT_REJECTED'::text, 'VERIFIED'::text, 'REGISTERED'::text, 'REFUND_PENDING'::text, 'REFUND_COMPLETE'::text])),
  method text NOT NULL DEFAULT 'MANUAL'::text CHECK (method = ANY (ARRAY['MANUAL'::text, 'RAZORPAY'::text, 'ERP_GATEWAY'::text])),
  amount numeric NOT NULL DEFAULT 0 CHECK (amount >= 0::numeric),
  proof_url text NOT NULL DEFAULT ''::text,
  proof_object_key text NOT NULL DEFAULT ''::text,
  razorpay_payment_id text NOT NULL DEFAULT ''::text,
  refund_status text NOT NULL DEFAULT 'NONE'::text CHECK (refund_status = ANY (ARRAY['NONE'::text, 'PENDING'::text, 'COMPLETE'::text])),
  rejection_reason text NOT NULL DEFAULT ''::text,
  verified_by_auth_user_id uuid,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  submitted_at timestamp with time zone,
  verified_at timestamp with time zone,
  razorpay_order_id text NOT NULL DEFAULT ''::text,
  gateway_name text NOT NULL DEFAULT ''::text,
  CONSTRAINT event_payment_records_pkey PRIMARY KEY (payment_record_id),
  CONSTRAINT event_payment_records_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_payment_records_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_payment_records_verified_by_auth_user_id_fkey FOREIGN KEY (verified_by_auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_payment_records_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_payment_records_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_payment_records_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(comp_id),
  CONSTRAINT event_payment_records_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(comp_id)
);
CREATE TABLE public.event_registrations (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  student_id text NOT NULL,
  auth_user_id uuid,
  legacy_firebase_uid text NOT NULL DEFAULT ''::text,
  mode text NOT NULL CHECK (mode = ANY (ARRAY['individual'::text, 'team'::text])),
  team_id text NOT NULL DEFAULT ''::text,
  role text NOT NULL DEFAULT 'MEMBER'::text CHECK (role = ANY (ARRAY['LEADER'::text, 'MEMBER'::text])),
  status text NOT NULL DEFAULT 'PENDING'::text CHECK (status = ANY (ARRAY['PENDING'::text, 'ACCEPTED'::text, 'REGISTERED'::text, 'PAYMENT_PENDING'::text, 'PAYMENT_SUBMITTED'::text, 'PAYMENT_REJECTED'::text, 'VERIFIED'::text, 'CANCELLED'::text])),
  category text NOT NULL DEFAULT ''::text,
  payment_status text NOT NULL DEFAULT ''::text,
  selected_topic text NOT NULL DEFAULT ''::text,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  registered_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  invited_by_student_id text,
  accepted_at timestamp with time zone,
  cancelled_at timestamp with time zone,
  previous_status text NOT NULL DEFAULT ''::text,
  payment_proof_url text NOT NULL DEFAULT ''::text,
  payment_submitted_at timestamp with time zone,
  payment_verified_at timestamp with time zone,
  payment_verified_by_auth_user_id uuid,
  rejection_reason text NOT NULL DEFAULT ''::text,
  CONSTRAINT event_registrations_pkey PRIMARY KEY (event_id, comp_id, student_id),
  CONSTRAINT event_registrations_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_registrations_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_registrations_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_registrations_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_registrations_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(comp_id),
  CONSTRAINT event_registrations_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(comp_id),
  CONSTRAINT event_registrations_invited_by_student_id_fkey FOREIGN KEY (invited_by_student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_registrations_payment_verified_by_auth_user_id_fkey FOREIGN KEY (payment_verified_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.event_results (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  status text NOT NULL DEFAULT 'PARTICIPANT'::text CHECK (status = ANY (ARRAY['WINNER'::text, 'RUNNER_UP'::text, 'SECOND_RUNNER_UP'::text, 'PARTICIPANT'::text])),
  rank_no integer NOT NULL DEFAULT 0 CHECK (rank_no >= 0),
  score numeric,
  max_score numeric,
  members_json jsonb NOT NULL DEFAULT '[]'::jsonb,
  published boolean NOT NULL DEFAULT false,
  published_at timestamp with time zone,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_results_pkey PRIMARY KEY (event_id, comp_id, team_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_results_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(team_id)
);
CREATE TABLE public.event_scanners (
  event_id text NOT NULL,
  student_id text NOT NULL,
  auth_user_id uuid,
  legacy_firebase_uid text NOT NULL DEFAULT ''::text,
  added_by_auth_user_id uuid,
  added_at timestamp with time zone NOT NULL DEFAULT now(),
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  CONSTRAINT event_scanners_pkey PRIMARY KEY (event_id, student_id),
  CONSTRAINT event_scanners_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_core(event_id),
  CONSTRAINT event_scanners_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_scanners_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_scanners_added_by_auth_user_id_fkey FOREIGN KEY (added_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.event_schedule_stages (
  event_id text NOT NULL,
  stage_id text NOT NULL,
  title text NOT NULL,
  type text NOT NULL DEFAULT 'CUSTOM'::text,
  status text NOT NULL DEFAULT 'UPCOMING'::text CHECK (status = ANY (ARRAY['UPCOMING'::text, 'ACTIVE'::text, 'COMPLETED'::text, 'CANCELLED'::text])),
  stage_order integer NOT NULL DEFAULT 0,
  is_public boolean NOT NULL DEFAULT true,
  starts_at timestamp with time zone,
  ends_at timestamp with time zone,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_schedule_stages_pkey PRIMARY KEY (event_id, stage_id),
  CONSTRAINT event_schedule_stages_event_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_core(event_id)
);
CREATE TABLE public.event_team_invites (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  student_id text NOT NULL,
  invited_by_student_id text,
  status text NOT NULL DEFAULT 'PENDING'::text CHECK (status = ANY (ARRAY['PENDING'::text, 'ACCEPTED'::text, 'REJECTED'::text, 'CANCELLED'::text, 'EXPIRED'::text])),
  inbox_id text NOT NULL DEFAULT ''::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  responded_at timestamp with time zone,
  CONSTRAINT event_team_invites_pkey PRIMARY KEY (event_id, comp_id, team_id, student_id),
  CONSTRAINT event_team_invites_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_team_invites_invited_by_student_id_fkey FOREIGN KEY (invited_by_student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_team_invites_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(team_id)
);
CREATE TABLE public.event_team_members (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  student_id text NOT NULL,
  auth_user_id uuid,
  role text NOT NULL DEFAULT 'MEMBER'::text CHECK (role = ANY (ARRAY['LEADER'::text, 'MEMBER'::text])),
  status text NOT NULL DEFAULT 'ACTIVE'::text CHECK (status = ANY (ARRAY['ACTIVE'::text, 'INVITED'::text, 'PENDING'::text, 'LEFT'::text, 'REMOVED'::text])),
  joined_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_team_members_pkey PRIMARY KEY (event_id, comp_id, team_id, student_id),
  CONSTRAINT event_team_members_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_team_members_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(event_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(comp_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_teams(team_id),
  CONSTRAINT event_team_members_event_id_comp_id_team_id_fkey FOREIGN KEY (team_id) REFERENCES public.event_teams(team_id)
);
CREATE TABLE public.event_teams (
  event_id text NOT NULL,
  comp_id text NOT NULL,
  team_id text NOT NULL,
  team_name text NOT NULL DEFAULT ''::text,
  leader_student_id text,
  leader_auth_user_id uuid,
  participant_type text NOT NULL DEFAULT 'TEAM'::text CHECK (participant_type = ANY (ARRAY['SOLO'::text, 'TEAM'::text])),
  team_status text NOT NULL DEFAULT 'INCOMPLETE'::text CHECK (team_status = ANY (ARRAY['OPEN'::text, 'INCOMPLETE'::text, 'COMPLETE'::text, 'LOCKED'::text, 'CANCELLED'::text])),
  status text NOT NULL DEFAULT 'OPEN'::text,
  max_members integer NOT NULL DEFAULT 1 CHECK (max_members >= 1 AND max_members <= 50),
  active boolean NOT NULL DEFAULT true,
  team_locked boolean NOT NULL DEFAULT false,
  selected_topic text NOT NULL DEFAULT ''::text,
  topic_locked boolean NOT NULL DEFAULT false,
  topic_submitted_at timestamp with time zone,
  payment_status text NOT NULL DEFAULT ''::text,
  category text NOT NULL DEFAULT ''::text,
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT event_teams_pkey PRIMARY KEY (event_id, comp_id, team_id),
  CONSTRAINT event_teams_leader_student_id_fkey FOREIGN KEY (leader_student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT event_teams_leader_auth_user_id_fkey FOREIGN KEY (leader_auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT event_teams_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_teams_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(event_id),
  CONSTRAINT event_teams_event_id_comp_id_fkey FOREIGN KEY (event_id) REFERENCES public.event_competitions(comp_id),
  CONSTRAINT event_teams_event_id_comp_id_fkey FOREIGN KEY (comp_id) REFERENCES public.event_competitions(comp_id)
);
CREATE TABLE public.feedback_cycle_entries (
  cycle_id text NOT NULL,
  entry_id text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  teacher_id text NOT NULL DEFAULT ''::text,
  teacher_name text NOT NULL DEFAULT ''::text,
  teacher_subject text NOT NULL DEFAULT ''::text,
  teacher_photo_url text NOT NULL DEFAULT ''::text,
  subject_type text NOT NULL DEFAULT 'THEORY'::text CHECK (subject_type = ANY (ARRAY['THEORY'::text, 'PRACTICAL'::text])),
  template_id text NOT NULL DEFAULT ''::text,
  template_name text NOT NULL DEFAULT ''::text,
  rating_scale integer NOT NULL DEFAULT 5 CHECK (rating_scale = ANY (ARRAY[5, 10])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_cycle_entries_pkey PRIMARY KEY (cycle_id, entry_id),
  CONSTRAINT feedback_cycle_entries_cycle_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycles(cycle_id)
);
CREATE TABLE public.feedback_cycle_entry_questions (
  cycle_id text NOT NULL,
  entry_id text NOT NULL,
  question_id text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  question_text text NOT NULL DEFAULT ''::text,
  rating_scale integer NOT NULL DEFAULT 5 CHECK (rating_scale = ANY (ARRAY[5, 10])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_cycle_entry_questions_pkey PRIMARY KEY (cycle_id, entry_id, question_id),
  CONSTRAINT feedback_cycle_entry_questions_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_cycle_entry_questions_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_cycle_entry_questions_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(entry_id),
  CONSTRAINT feedback_cycle_entry_questions_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(entry_id)
);
CREATE TABLE public.feedback_cycles (
  cycle_id text NOT NULL,
  cycle_name text NOT NULL DEFAULT ''::text,
  header_label text NOT NULL DEFAULT ''::text,
  course text NOT NULL DEFAULT 'ALL'::text,
  course_key text NOT NULL DEFAULT 'all'::text,
  branch text NOT NULL DEFAULT 'ALL'::text,
  branch_key text NOT NULL DEFAULT 'all'::text,
  semester text NOT NULL DEFAULT 'ALL'::text,
  semester_key text NOT NULL DEFAULT 'all'::text,
  target_key text NOT NULL DEFAULT 'all_students'::text,
  audience_type text NOT NULL DEFAULT 'STUDENT'::text,
  status text NOT NULL DEFAULT 'DRAFT'::text CHECK (status = ANY (ARRAY['DRAFT'::text, 'PUBLISHED'::text, 'CLOSED'::text])),
  active boolean NOT NULL DEFAULT true,
  entry_count integer NOT NULL DEFAULT 0 CHECK (entry_count >= 0),
  created_by_auth_user_id uuid,
  published_at timestamp with time zone,
  expires_at timestamp with time zone,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_cycles_pkey PRIMARY KEY (cycle_id),
  CONSTRAINT feedback_cycles_created_by_auth_user_id_fkey FOREIGN KEY (created_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.feedback_responses (
  response_id uuid NOT NULL DEFAULT gen_random_uuid(),
  cycle_id text NOT NULL,
  entry_id text NOT NULL,
  teacher_id text NOT NULL DEFAULT ''::text,
  subject_type text NOT NULL DEFAULT 'THEORY'::text CHECK (subject_type = ANY (ARRAY['THEORY'::text, 'PRACTICAL'::text])),
  rating_scale integer NOT NULL DEFAULT 5 CHECK (rating_scale = ANY (ARRAY[5, 10])),
  prompt_count integer NOT NULL DEFAULT 0 CHECK (prompt_count >= 0 AND prompt_count <= 60),
  optional_comment text NOT NULL DEFAULT ''::text CHECK (char_length(optional_comment) <= 500),
  answers_jsonb jsonb NOT NULL DEFAULT '[]'::jsonb CHECK (jsonb_typeof(answers_jsonb) = 'array'::text),
  submitted_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_responses_pkey PRIMARY KEY (response_id),
  CONSTRAINT feedback_responses_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_responses_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_responses_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(entry_id),
  CONSTRAINT feedback_responses_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(entry_id)
);
CREATE TABLE public.feedback_submissions (
  cycle_id text NOT NULL,
  entry_id text NOT NULL,
  uid text NOT NULL,
  auth_user_id uuid NOT NULL,
  submitted boolean NOT NULL DEFAULT true,
  submitted_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_submissions_pkey PRIMARY KEY (cycle_id, entry_id, uid),
  CONSTRAINT feedback_submissions_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT feedback_submissions_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_submissions_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(cycle_id),
  CONSTRAINT feedback_submissions_cycle_id_entry_id_fkey FOREIGN KEY (cycle_id) REFERENCES public.feedback_cycle_entries(entry_id),
  CONSTRAINT feedback_submissions_cycle_id_entry_id_fkey FOREIGN KEY (entry_id) REFERENCES public.feedback_cycle_entries(entry_id)
);
CREATE TABLE public.feedback_teachers (
  teacher_id text NOT NULL,
  name text NOT NULL DEFAULT ''::text,
  name_key text NOT NULL DEFAULT ''::text,
  subject text NOT NULL DEFAULT ''::text,
  subject_key text NOT NULL DEFAULT ''::text,
  photo_url text NOT NULL DEFAULT ''::text,
  photo_path text NOT NULL DEFAULT ''::text,
  active boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_teachers_pkey PRIMARY KEY (teacher_id)
);
CREATE TABLE public.feedback_template_questions (
  template_id text NOT NULL,
  question_id text NOT NULL,
  position integer NOT NULL DEFAULT 0,
  question_text text NOT NULL DEFAULT ''::text,
  rating_scale integer NOT NULL DEFAULT 5 CHECK (rating_scale = ANY (ARRAY[5, 10])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_template_questions_pkey PRIMARY KEY (template_id, question_id),
  CONSTRAINT feedback_template_questions_template_id_fkey FOREIGN KEY (template_id) REFERENCES public.feedback_templates(template_id)
);
CREATE TABLE public.feedback_templates (
  template_id text NOT NULL,
  template_name text NOT NULL DEFAULT ''::text,
  department_name text NOT NULL DEFAULT ''::text,
  department_key text NOT NULL DEFAULT ''::text,
  branch text NOT NULL DEFAULT 'ALL'::text,
  branch_key text NOT NULL DEFAULT 'all'::text,
  status text NOT NULL DEFAULT 'DRAFT'::text CHECK (status = ANY (ARRAY['DRAFT'::text, 'ACTIVE'::text])),
  rating_scale integer NOT NULL DEFAULT 5 CHECK (rating_scale = ANY (ARRAY[5, 10])),
  created_by_auth_user_id uuid,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT feedback_templates_pkey PRIMARY KEY (template_id),
  CONSTRAINT feedback_templates_created_by_auth_user_id_fkey FOREIGN KEY (created_by_auth_user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.practice_question_bank (
  question_id text NOT NULL CHECK (length(TRIM(BOTH FROM question_id)) > 0),
  subject text NOT NULL CHECK (length(TRIM(BOTH FROM subject)) > 0),
  unit text NOT NULL DEFAULT ''::text,
  topic text NOT NULL CHECK (length(TRIM(BOTH FROM topic)) > 0),
  question_text text NOT NULL CHECK (length(TRIM(BOTH FROM question_text)) > 0),
  option_a text NOT NULL DEFAULT ''::text,
  option_b text NOT NULL DEFAULT ''::text,
  option_c text NOT NULL DEFAULT ''::text,
  option_d text NOT NULL DEFAULT ''::text,
  answer_key text NOT NULL CHECK (answer_key = ANY (ARRAY['A'::text, 'B'::text, 'C'::text, 'D'::text])),
  difficulty text NOT NULL DEFAULT ''::text,
  solution text NOT NULL DEFAULT ''::text,
  active boolean NOT NULL DEFAULT true,
  pools ARRAY NOT NULL DEFAULT ARRAY['practice'::text],
  solution_mode text NOT NULL DEFAULT 'full'::text,
  question_order integer NOT NULL DEFAULT 0,
  shuffle_bucket integer NOT NULL DEFAULT 0 CHECK (shuffle_bucket >= 0 AND shuffle_bucket <= 999999),
  source_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT practice_question_bank_pkey PRIMARY KEY (question_id)
);
CREATE TABLE public.stu_branch_master (
  stu_branch_id bigint NOT NULL DEFAULT nextval('stu_branch_master_stu_branch_id_seq'::regclass),
  stu_branch_code text NOT NULL,
  stu_branch_name text NOT NULL,
  stu_branch_course_id bigint NOT NULL,
  stu_branch_campus_id bigint NOT NULL,
  stu_branch_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_branch_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stu_branch_master_pkey PRIMARY KEY (stu_branch_id),
  CONSTRAINT stu_branch_master_stu_branch_course_id_fkey FOREIGN KEY (stu_branch_course_id) REFERENCES public.stu_course_master(stu_course_id),
  CONSTRAINT stu_branch_master_stu_branch_campus_id_fkey FOREIGN KEY (stu_branch_campus_id) REFERENCES public.stu_campus_master(stu_campus_id)
);
CREATE TABLE public.stu_campus_master (
  stu_campus_id bigint NOT NULL DEFAULT nextval('stu_campus_master_stu_campus_id_seq'::regclass),
  stu_campus_code text NOT NULL UNIQUE,
  stu_campus_name text NOT NULL,
  stu_campus_state text,
  stu_campus_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_campus_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stu_campus_master_pkey PRIMARY KEY (stu_campus_id)
);
CREATE TABLE public.stu_course_master (
  stu_course_id bigint NOT NULL DEFAULT nextval('stu_course_master_stu_course_id_seq'::regclass),
  stu_course_code text NOT NULL UNIQUE,
  stu_course_name text NOT NULL,
  stu_course_level text NOT NULL CHECK (stu_course_level = ANY (ARRAY['UG'::text, 'PG'::text, 'PHD'::text, 'DIPLOMA'::text, 'OTHER'::text])),
  stu_course_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_course_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stu_course_master_pkey PRIMARY KEY (stu_course_id)
);
CREATE TABLE public.stu_section_master (
  stu_section_id bigint NOT NULL DEFAULT nextval('stu_section_master_stu_section_id_seq'::regclass),
  stu_section_code text NOT NULL UNIQUE,
  stu_section_name text NOT NULL,
  stu_section_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_section_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stu_section_master_pkey PRIMARY KEY (stu_section_id)
);
CREATE TABLE public.stu_specialization_master (
  stu_specialization_id bigint NOT NULL DEFAULT nextval('stu_specialization_master_stu_specialization_id_seq'::regclass),
  stu_specialization_code text NOT NULL UNIQUE,
  stu_specialization_name text NOT NULL,
  stu_specialization_course_id bigint,
  stu_specialization_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_specialization_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT stu_specialization_master_pkey PRIMARY KEY (stu_specialization_id),
  CONSTRAINT stu_specialization_master_stu_specialization_course_id_fkey FOREIGN KEY (stu_specialization_course_id) REFERENCES public.stu_course_master(stu_course_id)
);
CREATE TABLE public.student_academic_record (
  stu_academic_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_academic_student_id text NOT NULL,
  stu_level_code text NOT NULL CHECK (stu_level_code = ANY (ARRAY['HIGH_SCHOOL'::text, 'INTERMEDIATE'::text, 'BACHELOR'::text])),
  stu_board_or_university_name text,
  stu_institute_name text,
  stu_passing_year integer CHECK (stu_passing_year >= 1950 AND stu_passing_year <= 2100),
  stu_score_type text CHECK (stu_score_type = ANY (ARRAY['PERCENT'::text, 'CGPA'::text])),
  stu_score_value numeric CHECK (stu_score_value >= 0::numeric AND stu_score_value <= 100::numeric),
  stu_subject_major text,
  stu_academic_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_academic_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_academic_record_pkey PRIMARY KEY (stu_academic_id),
  CONSTRAINT student_academic_record_stu_academic_student_id_fkey FOREIGN KEY (stu_academic_student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_address (
  stu_address_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_address_student_id text NOT NULL UNIQUE,
  stu_permanent_line1 text,
  stu_permanent_line2 text,
  stu_city_name text,
  stu_state_name text,
  stu_pin_code text,
  stu_country_name text,
  stu_address_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_address_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_address_pkey PRIMARY KEY (stu_address_id),
  CONSTRAINT student_address_stu_address_student_id_fkey FOREIGN KEY (stu_address_student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_attendance_daily (
  stu_attendance_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_attendance_student_id text NOT NULL,
  stu_attendance_date date NOT NULL,
  stu_attendance_mark text NOT NULL CHECK (stu_attendance_mark = ANY (ARRAY['P'::text, 'A'::text, 'L'::text, 'OD'::text])),
  stu_attendance_marked_by text,
  stu_attendance_created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_attendance_daily_pkey PRIMARY KEY (stu_attendance_id),
  CONSTRAINT student_attendance_daily_stu_attendance_student_id_fkey FOREIGN KEY (stu_attendance_student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_contact (
  stu_contact_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_contact_student_id text NOT NULL UNIQUE,
  stu_official_email text NOT NULL UNIQUE CHECK (POSITION(('@'::text) IN (stu_official_email)) > 1),
  stu_personal_email text UNIQUE CHECK (stu_personal_email IS NULL OR POSITION(('@'::text) IN (stu_personal_email)) > 1),
  stu_primary_phone text NOT NULL,
  stu_alternate_phone text,
  stu_contact_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_contact_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_contact_pkey PRIMARY KEY (stu_contact_id),
  CONSTRAINT student_contact_stu_contact_student_id_fkey FOREIGN KEY (stu_contact_student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_core (
  stu_student_id text NOT NULL CHECK (length(TRIM(BOTH FROM stu_student_id)) > 0),
  stu_auth_user_uuid uuid UNIQUE,
  stu_full_name text NOT NULL CHECK (length(TRIM(BOTH FROM stu_full_name)) > 1),
  stu_dob date NOT NULL CHECK (stu_dob <= CURRENT_DATE),
  stu_gender_code text CHECK (stu_gender_code = ANY (ARRAY['MALE'::text, 'FEMALE'::text, 'OTHER'::text, 'UNDISCLOSED'::text])),
  stu_category_code text CHECK (stu_category_code = ANY (ARRAY['GEN'::text, 'OBC'::text, 'SC'::text, 'ST'::text, 'EWS'::text, 'OTHER'::text])),
  stu_ubi_code text UNIQUE,
  stu_profile_photo_url text,
  stu_profile_photo_storage_provider text,
  stu_profile_photo_object_key text,
  stu_profile_photo_updated_at timestamp with time zone,
  stu_account_state text NOT NULL DEFAULT 'ACTIVE'::text CHECK (stu_account_state = ANY (ARRAY['ACTIVE'::text, 'INACTIVE'::text, 'ALUMNI'::text, 'SUSPENDED'::text, 'BLOCKED'::text])),
  stu_core_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_core_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_core_pkey PRIMARY KEY (stu_student_id),
  CONSTRAINT student_core_stu_auth_user_uuid_fkey FOREIGN KEY (stu_auth_user_uuid) REFERENCES auth.users(id)
);
CREATE TABLE public.student_enrollment_current (
  stu_enroll_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_enroll_student_id text NOT NULL UNIQUE,
  stu_college_campus_id bigint NOT NULL,
  stu_course_id bigint NOT NULL,
  stu_specialization_id bigint,
  stu_branch_id bigint,
  stu_section_id bigint,
  stu_year_sem_no integer NOT NULL CHECK (stu_year_sem_no >= 1 AND stu_year_sem_no <= 12),
  stu_class_roll_no integer,
  stu_enroll_no text UNIQUE,
  stu_university_roll_no text UNIQUE,
  stu_enroll_state text NOT NULL DEFAULT 'ACTIVE'::text CHECK (stu_enroll_state = ANY (ARRAY['ACTIVE'::text, 'INACTIVE'::text, 'SUSPENDED'::text, 'PASSED_OUT'::text])),
  stu_enroll_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_enroll_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_enrollment_current_pkey PRIMARY KEY (stu_enroll_id),
  CONSTRAINT student_enrollment_current_stu_enroll_student_id_fkey FOREIGN KEY (stu_enroll_student_id) REFERENCES public.student_core(stu_student_id),
  CONSTRAINT student_enrollment_current_stu_college_campus_id_fkey FOREIGN KEY (stu_college_campus_id) REFERENCES public.stu_campus_master(stu_campus_id),
  CONSTRAINT student_enrollment_current_stu_course_id_fkey FOREIGN KEY (stu_course_id) REFERENCES public.stu_course_master(stu_course_id),
  CONSTRAINT student_enrollment_current_stu_specialization_id_fkey FOREIGN KEY (stu_specialization_id) REFERENCES public.stu_specialization_master(stu_specialization_id),
  CONSTRAINT student_enrollment_current_stu_branch_id_fkey FOREIGN KEY (stu_branch_id) REFERENCES public.stu_branch_master(stu_branch_id),
  CONSTRAINT student_enrollment_current_stu_section_id_fkey FOREIGN KEY (stu_section_id) REFERENCES public.stu_section_master(stu_section_id)
);
CREATE TABLE public.student_parent (
  stu_parent_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_parent_student_id text NOT NULL UNIQUE,
  stu_father_name text,
  stu_mother_name text,
  stu_father_phone text,
  stu_mother_phone text,
  stu_father_occupation text,
  stu_mother_occupation text,
  stu_parent_created_at timestamp with time zone NOT NULL DEFAULT now(),
  stu_parent_updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_parent_pkey PRIMARY KEY (stu_parent_id),
  CONSTRAINT student_parent_stu_parent_student_id_fkey FOREIGN KEY (stu_parent_student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_resume_profile (
  uid text NOT NULL,
  auth_user_id uuid,
  student_id text,
  default_resume_id text NOT NULL DEFAULT ''::text,
  source_legacy_uid text NOT NULL DEFAULT ''::text,
  source_payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_resume_profile_pkey PRIMARY KEY (uid),
  CONSTRAINT student_resume_profile_uid_fkey FOREIGN KEY (uid) REFERENCES public.app_profile_state(uid),
  CONSTRAINT student_resume_profile_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT student_resume_profile_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_resumes (
  uid text NOT NULL,
  resume_id text NOT NULL,
  auth_user_id uuid,
  student_id text,
  resume_name text NOT NULL DEFAULT 'My Resume'::text,
  template_id text NOT NULL DEFAULT 'classic'::text,
  is_default boolean NOT NULL DEFAULT false,
  last_updated_ms bigint NOT NULL DEFAULT 0,
  domain text NOT NULL DEFAULT 'Tech'::text,
  target_role text NOT NULL DEFAULT ''::text,
  experience_level text NOT NULL DEFAULT 'Fresher'::text,
  strictness_mode text NOT NULL DEFAULT 'Product'::text,
  full_name text NOT NULL DEFAULT ''::text,
  email text NOT NULL DEFAULT ''::text,
  phone text NOT NULL DEFAULT ''::text,
  payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  ats_snapshot_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  source_payload_json jsonb NOT NULL DEFAULT '{}'::jsonb,
  deleted_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_resumes_pkey PRIMARY KEY (uid, resume_id),
  CONSTRAINT student_resumes_uid_fkey FOREIGN KEY (uid) REFERENCES public.app_profile_state(uid),
  CONSTRAINT student_resumes_auth_user_id_fkey FOREIGN KEY (auth_user_id) REFERENCES auth.users(id),
  CONSTRAINT student_resumes_student_id_fkey FOREIGN KEY (student_id) REFERENCES public.student_core(stu_student_id)
);
CREATE TABLE public.student_status_log (
  stu_status_log_id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  stu_status_student_id text NOT NULL,
  stu_prev_state text,
  stu_next_state text NOT NULL,
  stu_state_reason text,
  stu_state_changed_by text,
  stu_state_changed_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT student_status_log_pkey PRIMARY KEY (stu_status_log_id),
  CONSTRAINT student_status_log_stu_status_student_id_fkey FOREIGN KEY (stu_status_student_id) REFERENCES public.student_core(stu_student_id)
);